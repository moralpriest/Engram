//go:build amd64.v3

#include "textflag.h"

TEXT ·buildEqualColumns(SB), NOSPLIT, $0-24
	MOVQ first+0(FP), AX
	MOVL groupCount+8(FP), CX
	MOVQ out+16(FP), DX
	VPCMPEQB Y1, Y1, Y1
	VPCMPEQB Y2, Y2, Y2
	VPCMPEQB Y3, Y3, Y3
	VPCMPEQB Y4, Y4, Y4
	VPCMPEQB Y5, Y5, Y5
	VPCMPEQB Y6, Y6, Y6
	VPCMPEQB Y7, Y7, Y7
	VPCMPEQB Y8, Y8, Y8
	SUBL $1, CX
	JLE done
	LEAQ 256(AX), BX
loop:
	VMOVDQU 0(BX), Y0
	VPCMPEQB 0(AX), Y0, Y0
	VPAND Y0, Y1, Y1
	VMOVDQU 32(BX), Y0
	VPCMPEQB 32(AX), Y0, Y0
	VPAND Y0, Y2, Y2
	VMOVDQU 64(BX), Y0
	VPCMPEQB 64(AX), Y0, Y0
	VPAND Y0, Y3, Y3
	VMOVDQU 96(BX), Y0
	VPCMPEQB 96(AX), Y0, Y0
	VPAND Y0, Y4, Y4
	VMOVDQU 128(BX), Y0
	VPCMPEQB 128(AX), Y0, Y0
	VPAND Y0, Y5, Y5
	VMOVDQU 160(BX), Y0
	VPCMPEQB 160(AX), Y0, Y0
	VPAND Y0, Y6, Y6
	VMOVDQU 192(BX), Y0
	VPCMPEQB 192(AX), Y0, Y0
	VPAND Y0, Y7, Y7
	VMOVDQU 224(BX), Y0
	VPCMPEQB 224(AX), Y0, Y0
	VPAND Y0, Y8, Y8
	ADDQ $256, BX
	SUBL $1, CX
	JG loop
done:
	VPMOVMSKB Y1, R8
	VPMOVMSKB Y2, R9
	VPMOVMSKB Y3, R10
	VPMOVMSKB Y4, R11
	VPMOVMSKB Y5, R12
	VPMOVMSKB Y6, R13
	VPMOVMSKB Y7, R14
	VPMOVMSKB Y8, R15
	MOVL R8, 0(DX)
	MOVL R9, 4(DX)
	MOVL R10, 8(DX)
	MOVL R11, 12(DX)
	MOVL R12, 16(DX)
	MOVL R13, 20(DX)
	MOVL R14, 24(DX)
	MOVL R15, 28(DX)
	VZEROUPPER
	RET
